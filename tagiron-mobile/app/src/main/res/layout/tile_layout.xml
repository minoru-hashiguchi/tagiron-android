<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <import type="com.icloud.hashiguchi.minoru.tagiron.Tile" />

        <import type="android.view.View" />

        <variable
            name="viewmodel"
            type="com.icloud.hashiguchi.minoru.mobile.data.GameLiveDataViewModel" />

        <variable
            name="tile"
            type="com.icloud.hashiguchi.minoru.tagiron.Tile" />

        <variable
            name="position"
            type="Integer" />
    </data>


    <LinearLayout

        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:background="@color/black"
            android:gravity="center_horizontal"
            android:onClick="@{() -> viewmodel.onClickTile(position)}"
            android:orientation="vertical"
            android:padding="5dp">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center_horizontal|top"
                android:text="@{position + `番目`}"
                android:textSize="10sp" />

            <TextView
                android:id="@+id/textViewNo"
                textViewBackground="@{tile.colorStr}"
                android:layout_width="match_parent"
                android:layout_height="90dp"
                android:layout_gravity="center"
                android:gravity="center"
                android:text="@{tile.noText ?? `-`}"
                android:textColor="@color/white"
                android:textSize="50sp"
                android:textStyle="bold" />

        </LinearLayout>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_horizontal|top"
            android:text="@{`▲`}"
            android:textColor="@color/black"
            android:textSize="20sp"
            android:visibility="@{(viewmodel.selectedTilePosition == position &amp;&amp; viewmodel.showCallEditor)? View.VISIBLE : View.GONE}" />
    </LinearLayout>
</layout>

